@page "/test"
@inject ReportRequestService ReportRequestService
@inject NotificationService NotificationService
@inject AssignmentService  AssignmentService
@inject UserService UserService

<ReportSurveyInput ReportAssignment=@ass/>

@code {
    ReportAssignment ass;

    protected override void OnInitialized()
    {
        var a = AssignmentService.GetForUser(UserService.GetCurrentUserModel(), true);
        //ass =  a.FirstOrDefault(a => a.ReportRequestSurvey is not null);
    }

}
